while True:
    # Come um pedaço de bolo (faz alguma coisa)
    print("A emely aceita namorar com o joão")
    
    # Verifica se ainda tem bolo
    tem_bolo = input("Tem alguma esperança? (s/n) ")
    
    # Se não tiver mais bolo, paramos
    if tem_bolo.lower() == 'n':
        break